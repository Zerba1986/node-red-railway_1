module.exports = {
    uiPort: process.env.PORT || 3000,
    uiHost: "0.0.0.0",

    credentialSecret: "minha-chave-secreta",

    editorTheme: {
        projects: {
            enabled: true
        }
    }
}
